[pytest]
testpaths = tests
python_files = test_*.py
asyncio_mode = auto
markers =
    slow: Tests that take longer than usual (can be skipped with -m "not slow")
    integration: Tests requiring external services (e.g., Ollama)
addopts = 
    -v
    --strict-markers
